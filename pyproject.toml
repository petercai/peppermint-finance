[project]
name = "peppermint-root"
version = "0.1.0"
description = ""
authors = [
    {name = "Peter Cai",email = "petercaica@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
packages = [
    { include = "peppermint_stock" },
    { include = "openbb_platform"}
    ]

dependencies = []

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
